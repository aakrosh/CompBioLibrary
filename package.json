{
  "name": "compbiolibrary",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "present": "astro build && cd dist && reveal-notes-server"
  },
  "dependencies": {
    "astro": "^5.3.0",
    "reveal.js": "^5.2.1"
  },
  "devDependencies": {
    "reveal-notes-server": "^0.2.1"
  }
}
